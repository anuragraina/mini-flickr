import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
	listbox: {
		maxHeight: 'none',
	},
	options: {
		padding: '3px 9px',
	},
	optionIcon: {
		paddingRight: '12px',
	},
});

export default useStyles;
